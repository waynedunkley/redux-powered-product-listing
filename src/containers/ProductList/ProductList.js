import React from "react"
import styled from "styled-components"
import divStyles from "./ProductList.style"

const StyledDiv = styled.div`
  ${divStyles};
`

const ProductList = () => <StyledDiv />

export default ProductList
