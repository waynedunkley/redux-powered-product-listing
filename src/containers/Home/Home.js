import React from "react"
import { Container } from "components"
import { Header, Pagination } from "containers"

const Home = () => (
  <Container>
    <Header />
    <Pagination />
  </Container>
)

export default Home
